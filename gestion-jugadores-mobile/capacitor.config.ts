import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gestionjugadores.app',
  appName: 'Gestion Jugadores',
  webDir: 'www'
};

export default config;
